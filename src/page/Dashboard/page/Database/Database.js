import React, { useLable } from 'react'




const Database = props => {
    let { Wrapper } = useLable
    return (
        <Wrapper className="database-container">

        </Wrapper>
    )
}

export default Database;