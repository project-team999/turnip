import React, { useLable } from 'react'




const Sync = props => {
    let { Wrapper } = useLable
    return (
        <Wrapper className="sync-container">

        </Wrapper>
    )
}

export default Sync;