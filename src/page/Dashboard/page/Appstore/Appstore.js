import React, { useLable } from 'react'




const Appstore = props => {
    let { Wrapper } = useLable
    return (
        <Wrapper className="appstore-container">

        </Wrapper>
    )
}

export default Appstore;